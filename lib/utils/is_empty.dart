bool isEmpty(int firstIndex, int lastIndex, int index) {
  // print("isEmpty called");
  if (index >= firstIndex && lastIndex >= index) {
    // print("rohit ${true}");
    return true;
  } else {
    // print("rohit ${false}");
    return false;
  }
}
